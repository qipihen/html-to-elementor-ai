# Feedback Template For Fast Fix Loops

Use this exact structure when requesting fixes:

- URL:
- Taxonomy slug / term ID:
- Theme and version:
- WooCommerce version:
- Elementor version:
- Plugin render mode/hook:
- Visibility setting:
- Demo fallback toggles (category/detail):
- Data policy used (strict-empty or demo-fallback):
- Export check (field definitions imported? values populated?):
- Heading transform check (hero line vs intro line expected text):
- Empty-section check (which empty sections should be hidden entirely):

For each issue:
- Section:
- Expected:
- Actual:
- Screenshot:
- Browser console errors (if any):
- Related field values used:
